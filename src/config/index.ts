const config = {
  agora: {
    appId: process.env.NEXT_PUBLIC_AGORA_APP_ID,
    testToken: process.env.NEXT_PUBLIC_AGORA_TEST_TOKEN,
  },
  password: process.env.PASSWORD,
};

export default config;
